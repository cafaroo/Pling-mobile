// App root paths
export const APP_ROOT = '.';
export const COMPONENTS_ROOT = `${APP_ROOT}/components`;
export const CONTEXT_ROOT = `${APP_ROOT}/context`;
export const HOOKS_ROOT = `${APP_ROOT}/hooks`;
export const SERVICES_ROOT = `${APP_ROOT}/services`;
export const TYPES_ROOT = `${APP_ROOT}/types`;

// Component paths
export const UI_COMPONENTS = `${COMPONENTS_ROOT}/ui`;
export const TEAM_COMPONENTS = `${COMPONENTS_ROOT}/team`;
export const GOALS_COMPONENTS = `${COMPONENTS_ROOT}/goals`;

// Context paths
export const THEME_CONTEXT = `${CONTEXT_ROOT}/ThemeContext`;
export const USER_CONTEXT = `${CONTEXT_ROOT}/UserContext`;

// Service paths
export const TEAM_SERVICE = `${SERVICES_ROOT}/teamService`;
export const GOAL_SERVICE = `${SERVICES_ROOT}/goalService`;

// Hook paths
export const STYLES_HOOK = `${HOOKS_ROOT}/useStyles`; 